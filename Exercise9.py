# EXERCISE 9
# Fill in the blanks in the code below so that the function hello() prints "Hello" to the console.

def hello(): # the defined function will print "Hello" on the console
    print ("hello")
    
def main(): # the main function calls the hello function to execute
    hello()

if __name__ == "__main__": #this function checks if the code is run directly if not imported then calls main() to execute
    main()
